# URL base del backend HTTP (sin barra final).
# El proxy serverless en /api/* reenvía aquí y evita mixed content desde Vercel (HTTPS).
API_UPSTREAM=http://200.35.189.139

# Clave de API inyectada por el proxy (nunca se expone al navegador).
# En Vercel: Settings → Environment Variables.
API_KEY=
